// brand.ts — product name and shared brand strings for UI, metadata, and demo data.

export const BRAND_NAME = 'ManuMu Offshore' as const;

export const BRAND_DEMO_FLEET = `${BRAND_NAME} Demo Fleet` as const;

export const BRAND_HOME_LABEL = `${BRAND_NAME} home` as const;

export const BRAND_LOGO_LABEL = `${BRAND_NAME} logo` as const;

export const BRAND_PAGE_TITLE = `${BRAND_NAME} — Offshore Fixture Workflow Demo` as const;

export const BRAND_PAGE_DESCRIPTION =
  `${BRAND_NAME} models the full SUPPLYTIME offshore fixture cycle — vessel matching, subject-lift gating, marine weather evidence, and recap generation — on a seeded North Sea fleet.` as const;
