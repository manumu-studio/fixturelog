# FixtureLog voice worker — Python livekit-agents project (deploys to Railway).
# TASK-101 scope: transport proof only. Plugins are pinned now (TASK-102 wires them).

[project]
name = "fixturelog-voice-worker"
version = "0.1.0"
description = "FixtureLog Broker Copilot voice worker (LiveKit Agents)"
requires-python = ">=3.11,<3.13"
dependencies = [
    "livekit-agents[deepgram,anthropic,cartesia,silero,turn-detector]~=1.6",
    "python-dotenv>=1.0",
]

[tool.uv]
package = false
